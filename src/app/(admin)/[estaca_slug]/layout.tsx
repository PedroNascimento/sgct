/**
 * Área administrativa — placeholder para specs 001+ (RBAC completo).
 * O middleware já garantiu role e stake corretos antes de chegar aqui.
 */
export default async function AdminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
