/**
 * Tela de gestão de Estacas — Super Admin.
 * T000.18: permite cadastrar novas Estacas e ver as existentes.
 * Server Actions implementadas em actions.ts (spec 000).
 */
export default function EstacasPage() {
  return (
    <div>
      <h2 className="text-xl font-semibold text-gray-800 mb-4">Estacas Cadastradas</h2>
      <p className="text-sm text-gray-500">
        Interface de gestão de Estacas — a ser completada com Server Actions na spec 001.
      </p>
    </div>
  );
}
